import React from 'react';
import Navbar from '../component/Navbar';

const Home = () => {
    return (
        <div>
        <Navbar/>
            <h1>Accueil</h1>
        </div>
    );
};

export default Home;